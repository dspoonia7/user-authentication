# Angular 1.5 + ES6 + Webpack 2

## Usage

```shell
$ git clone https://github.com/dspoonia7/user-authentication
$ cd user-authentication
$ npm install
$ npm start
```

Now you can navigate to http://localhost:8080 to see the app working.
